import PlaceholderPage from "@/components/app/placeholder-page";

export default function PortfolioPage() {
  return <PlaceholderPage title="Portfolio" body="Holdings you add for tracking — not a broker connection — will live here." />;
}
