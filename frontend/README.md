# Quantora frontend

Next.js app for Quantora. The Spring Boot API is proxied through `/api`.

```bash
cp .env.example .env.local
npm install
npm run dev
```

Open http://localhost:3001 (port 3001 to avoid a local conflict on 3000).
